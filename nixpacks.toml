# nixpacks.toml

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader"
]

[phases.build]
cmds = [
  "php artisan config:clear",
  "php artisan storage:link"
]

[start]
cmd = "php -m 2>&1 && php artisan env 2>&1 && php artisan serve --host=0.0.0.0 --port=$PORT 2>&1"